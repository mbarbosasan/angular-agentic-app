import { ToContentPipe } from './to-content-pipe';

describe('ToContentPipe', () => {
  it('create an instance', () => {
    const pipe = new ToContentPipe();
    expect(pipe).toBeTruthy();
  });
});
